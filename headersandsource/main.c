// start of main
#include "multiply.h"  //no need semi-column
#include <stdio.h>

int main () {
    int result = multiply(2, 3);
    printf("great, result is %d", result);
    return 0;
}
//end of main.c
